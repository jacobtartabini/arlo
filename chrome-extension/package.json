{
  "name": "arlo-gmail-extension",
  "version": "1.0.0",
  "description": "Arlo for Gmail - Chrome Extension",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
